# The contents of this file is based on https://github.com/home-assistant/core/blob/dev/pyproject.toml

target-version = "py313" # matches homeassistant's Requires-Python; py314 makes the
# formatter emit PEP 758 bracketless `except A, B:`, a SyntaxError before 3.14

[lint]
select = [
    "ALL",
]

ignore = [
    "ANN401", # Dynamically typed expressions (typing.Any) are disallowed
    "D203", # no-blank-line-before-class (incompatible with formatter)
    "D212", # multi-line-summary-first-line (incompatible with formatter)
    "COM812", # incompatible with formatter
    "ISC001", # incompatible with formatter
    "CPY001", # no copyright headers in this project's files
]

[lint.flake8-pytest-style]
fixture-parentheses = false

[lint.pyupgrade]
keep-runtime-typing = true

[lint.mccabe]
max-complexity = 25

[lint.per-file-ignores]
# S101: assert is how pytest tests assert.
# SLF001: unit tests reach into hub/flow internals that have no public seam.
"tests/*" = ["S101", "SLF001"]
